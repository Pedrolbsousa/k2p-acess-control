{
  "name": "k2p-acess-control",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run start:dev --workspace=apps/api",
    "dev": "npm run dev:api & npm run dev:web",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  }
}